## Spartans Muay Thai — Website

Local development:

```bash
npm install
npm run dev
# open http://localhost:3000
```

Notes:
- Color theme pulled from `logo.svg`:
  - Primary (crimson): `#9b1f20`
  - Slate: `#3b3e4b`
  - Near-black: `#0a0203`
  - Base: `#ffffff`
- Typography:
  - Titles use the "Goodland" Bold font if available. Place files at:
    - `public/fonts/Goodland-Bold.woff2`
    - `public/fonts/Goodland-Bold.woff`
  - Fallbacks: system UI sans-serif.
- Home includes hero, session schedule, and placeholder private coaching cards.
- Update contact links and trainer info in `app/page.tsx`.

